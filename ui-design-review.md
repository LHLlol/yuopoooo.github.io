# UI Design Review

审查日期：2026-08-19  
审查对象：`http://127.0.0.1:5174/yuopoooo.github.io/`  
审查技能：`ui-design-review` 1.0.0  
当前实测视口：1059 × 1349

## Executive Summary

整体视觉方向明确：蓝白色彩、卡牌式作品滑块、渐变底部和轻量动效形成了统一的个人作品集识别度。主要问题集中在可读性与可访问性，而不是视觉风格本身：亮蓝文字在白底上对比度不足、辅助元数据过淡、部分控件触控区域偏小，以及首页电线交互无法通过键盘完成。

本轮已完成以下修复：

- 白底文字蓝从 `#08a9f4` 调整为 `#006d9f`，对比度从 2.63:1 提升到 5.69:1。
- `slate-400` 元数据色调整为 `#64748b`，白底对比度提升到 4.76:1。
- 添加“跳到主要内容”键盘入口。
- EXPERIENCE / HIGHLIGHTS 图片补充上下文 `alt` 文本。
- 卡牌滑块增加 `aria-roledescription="carousel"`。
- 首页电线增加键盘焦点、Enter/Space 操作和可读标签。
- 导航、AI 面板、CTA、底部链接统一补足约 44px 的触控高度。
- 精选作品滑块取消外层裁切，保留左右卡牌叠层的翻页空间。

## Findings

### HIGH — 文字对比度不足（已修复）

- 位置：`text-portfolioBlue`、`text-slate-400` 在白色信息卡和底部区域。
- 影响：低视力用户、低亮度屏幕和移动设备上阅读困难；不满足 WCAG 1.4.3 / 1.4.11。
- 修复：使用深青蓝语义色和更深的元数据灰；保留 `--portfolio-blue-rgb` 作为光效、渐变和装饰用途。

### HIGH — 图片缺少可访问上下文（已修复）

- 位置：EXPERIENCE / HIGHLIGHTS 的卡牌图片原先使用空 `alt` 并被父层 `aria-hidden` 隐藏。
- 影响：屏幕阅读器只能获取文字，无法知道卡牌右侧还有对应项目视觉资料；不满足 WCAG 1.1.1 的信息替代要求。
- 修复：使用组织名称和项目序号生成上下文 `alt` 文本，保留纯装饰图片的空 `alt` 策略。

### HIGH — 缺少跳过重复导航的入口（已修复）

- 位置：全站顶部固定导航。
- 影响：键盘用户每次进入页面都需要重复穿过导航；不满足 WCAG 2.4.1。
- 修复：新增聚焦时出现的“跳到主要内容”链接，并为 Home、Category、Project 页面主内容添加统一 `id`。

### MEDIUM — 触控目标偏小（已修复）

- 位置：导航文字链接、精选作品 CTA、AI 面板最小化/关闭/发送按钮、底部链接。
- 影响：触屏点击容错较低，尤其在移动端；对应 WCAG 2.5.5 的最佳实践要求。
- 修复：统一使用约 44px 的最小高度，保留原有视觉尺寸和圆角比例。

### MEDIUM — 自定义交互缺少键盘路径（已修复）

- 位置：首页 WireField 的 SVG 电线。
- 影响：鼠标可以悬停和点击，但键盘用户无法触发同一反馈。
- 修复：为每条电线添加 `role="button"`、可聚焦状态、中文 `aria-label` 以及 Enter/Space 操作。

### MEDIUM — 精选作品滑块的卡牌层被裁切（已修复）

- 位置：`.selected-works-deck-shell` 桌面端 `overflow: hidden`。
- 影响：前后卡牌、阴影和翻页位移被白色外层截断，视觉上不像 EXPERIENCE / HIGHLIGHTS 的卡牌式翻页。
- 修复：桌面端改为 `overflow: visible`，并为精选作品卡牌设置独立的层叠步进值。

## Visual / Layout Assessment

### 已确认的优点

- About 区域中的 EXPERIENCE / HIGHLIGHTS 已与整体容器左右边界对齐。
- 文字约占卡牌左侧三分之一，图片约占右侧三分之二，信息层级清楚。
- 作品图片使用白色内边距和 `object-fit: contain`，可完整呈现画面。
- 精选作品、经历和成就使用同一套圆角、阴影、箭头和拖拽模型。
- 页面标题层级保持 `h1 → h2 → h3` 的主要顺序。

### 后续建议

- 作品截图本身包含较多图片内文字，继续保留卡牌外部标题、类别和描述，不要只依赖图片中的文字传达项目信息。
- 发布前再用 320px、768px、1024px 和 1440px 视口分别检查卡牌叠层是否遮挡正文。
- 发布前补跑 Lighthouse / axe DevTools，并用 macOS VoiceOver 做一次完整的焦点顺序测试。

## Verification

- `npm run build`：通过。
- DOM 检查：存在 `#main-content`、skip link、轮播语义和可聚焦电线。
- 精选作品翻页：已验证活动索引可从 `0` 切换到 `1`。
- 当前工作区中的 `wangmumu` 子模块未参与本次修改。
